- name: Check URL
  uri:
    url: "http://192.168.10.59"
    method: GET
    status_code: 200
    timeout: 1

- name: Download index.nginx-debian.html
  get_url:
    url: http://192.168.10.59/index.nginx-debian.html
    dest: /tmp/test.html
    mode: '0440'

- name: Check if DB database exists
  mysql_db:
    name: "{{ db_name }}"
    state: present
